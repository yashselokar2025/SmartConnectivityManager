# AutoNetworkSwitcher

A FastAPI based intelligent network handler that detects WiFi disconnection and automatically switches to mobile data. Future feature includes auto recharge for small internet packs using payment integration.

### Features
- Detect WiFi loss
- Enable mobile data automatically
- Auto-recharge trigger (dummy for now)
- API based control (can evolve into background runnable service)

### Run Project

